export class Config {
  ROUTES: {
    BASE: string;
    REGISTER: string;
    LOGIN: string;
    SERVICES: string;
    REQUESTS: string;
    INSTANCES: string;
    LICENCES: string;
    PACKAGES: string;
  };
  PAGINATION: string;
}
